# 6.2 查询交互规范

查询是列表页最高频操作。

## 主查询

主查询区只放高频条件。

要求：

* 查询按钮必须明显
* 重置按钮紧跟查询
* 更多筛选入口必须可发现
* 输入后按 Enter 应可触发查询
* 查询结果应保持在当前页，不跳转

## 单号类查询

单号查询推荐采用：

```text
字段类型下拉 + 输入框
```

示例：

```text
业务单号 / 订单编号 / HBL / MBL / 柜号 / FBA / PO
```

避免为每一种单号都单独铺一个输入框。
