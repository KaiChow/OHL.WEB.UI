# 4.5 查询区域规范

## 主查询区

* 标签右对齐
* 控件等高
* 查询按钮突出
* 重置按钮弱化
* 更多筛选用文字按钮或轻量按钮

## 高级查询区

* 不使用大弹窗阻断列表操作
* 不一次性展示全部字段
* 使用分组切换
* 支持已选条件摘要
* 支持一键清空

## 单号查询

单号类查询推荐：

```text
单号类型下拉 + 输入框
```

避免每个单号类型各占一个输入框。
