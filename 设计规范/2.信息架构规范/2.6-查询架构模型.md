# 2.6 查询架构模型

查询分为三级。

## 高频查询

默认展示，控制在 1-2 行。

示例：

* 单号类型 + 关键字
* 业务类型
* 业务员
* 操作员
* 客户
* 状态

## 高级查询

页内展开，不建议弹窗打断流程。

高级查询必须按业务语义分组：

* 单号信息
* 客户与角色
* 运输与合同
* 业务标记
* 财务与文件

## 隐藏查询

低频、系统类、权限类条件进入更多配置或列设置。

## 禁止项

* 禁止把 20 个以上条件直接铺在首屏。
* 禁止高级查询一次性平铺几十个字段。
* 禁止每一种单号都单独占一个输入框。
