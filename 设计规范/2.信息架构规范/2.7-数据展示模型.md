# 2.7 数据展示模型

列表默认展示核心列，不超过 15 列。

推荐顺序：

```text
订单编号
状态
客户
业务员
起运港
目的港
ETD
ETA
柜量
柜号
HBL
MBL
文件
操作
```

## 字段过多处理方式

字段过多时，应采用：

* 列配置
* 横向滚动
* 详情抽屉
* 分组详情

## 表格信息层级

* 链接字段突出
* 状态字段使用 Tag 或状态点
* 次要字段降低视觉权重
* 操作列收敛为一个主操作

## 禁止项

* 禁止默认列表展示 20 列以上。
* 禁止所有字段同权重。
* 禁止把低频字段挤占核心字段空间。
