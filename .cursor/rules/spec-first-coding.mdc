---
description: UI 编码前按 freight skill 选择最小规范集
alwaysApply: true
---

# UI Spec-First Gate

涉及 `src/views/**`、`src/components/**` 时，以 `.agents/skills/freight-arco-ui/SKILL.md` 为唯一规则入口；禁止预读整个 references 目录。

## 写代码前

1. 所有 UI：`arco-first.md` + `theme-contract.md`。
2. 无设计稿（默认）：`existing-project-modernization.md` + `redesign-calibration.md` + 一个页面 authority。
3. 新页面、重写或重大布局/交互：先按 `page-spec-contract.md` 更新 typed `pageSpec.ts`。
4. 点击、请求、权限、状态或提交：`feature-delivery-contract.md` + 对应 surface authority。
5. 截图/Figma/原型：先完成 `prototype-to-ui-contract.md` 的统一 intake/translation block。
6. Icon 变更：`icons.md`；其他 surface 只读对应 authority。

## 实现顺序

Arco props/slots/structure -> GI/semantic tokens -> grep-proven shared pattern -> minimal page-local layout CSS. Shared class must exist in `src/styles/global.css`; reference examples are not APIs.

## 交付

Run `.cursor/rules/adversarial-review.mdc`, `node scripts/check-spec.js`, the relevant build, and real-route before/after verification when visual quality is in scope. A green script alone is not UI evidence.
