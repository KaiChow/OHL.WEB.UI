---
description: UI 交付前按商业产品标准做证据审查
alwaysApply: true
---

# UI Adversarial Review

结束 `src/views/**`、`src/components/**` UI/交互工作前，以拒绝发布的标准检查：

1. **业务可信**：对象、用户工作、关键状态/风险、责任人和下一动作是否真实，是否出现复制字段、假指标或通用 admin 语义？
2. **结构与效率**：`pageSpec.ts` 的 ownership 是否与真实路由一致；高频工作是否直达；主工作面、首屏比例、滚动和 overflow 是否通过所选 page/surface authority？
3. **交互闭环**：每个业务动作是否有显隐、可用、权限、pending/防重、成功、失败保留、刷新和危险确认；是否存在死按钮、猜测 API 或只做成功态？
4. **状态韧性**：适用的 loading、empty、permission、validation、business/network error、slow、long/extreme data、retry 和 partial failure 是否可确定复现并可恢复？
5. **产品一致性**：是否遵循 Arco/GI -> token -> shared pattern -> local layout，是否出现第二套皮肤、无依据共享类、页面私有组件语言或文档替代共享能力？
6. **真实证据**：是否检查真实路由 viewport、焦点、console、局部反馈和状态行为；是否只凭源码、截图、pageSpec 文案或绿色脚本自证？

融资、销售或产品化目标必须再逐项通过 `product-grade-evaluation.md` 的六个发布门。发现问题先修复；不能复现的状态和剩余风险必须明确报告，不能判定完成。
