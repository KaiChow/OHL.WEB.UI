---
description: UI 交付前按 freight skill 做证据审查
alwaysApply: true
---

# UI Adversarial Review

结束 `src/views/**`、`src/components/**` UI 工作前，以拒绝合并的标准检查：

1. `arco-first.md` + `theme-contract.md`: 是否优先使用 Arco/GI，是否出现第二套皮肤或无依据共享类？
2. `existing-project-modernization.md`: 无设计稿任务是否有真实路由 before/after，而非只报脚本通过？
3. `page-spec-contract.md`: material page work 是否有可测 PESDP acceptance，并与渲染证据一致？
4. 对应 surface authority：筛选、overlay、列表、表格、详情、动作是否只由各自 authority 决定？
5. `feature-delivery-contract.md`: 显隐、可用、权限、请求、失败保留、刷新和防重复提交是否完整？
6. `icons.md`: 图标库和作用域是否正确？
7. Artifact task: 是否先满足 `artifact-intake-template.md` 与 `prototype-to-ui-contract.md`？
8. 是否用新增文档掩盖缺失的共享 UI 能力、过量 page-local CSS 或未验证的真实效果？

发现问题先修复。最后运行 `node scripts/check-spec.js`；脚本不替代真实路由审查。
