---
description: UI 交付前对抗式审查；只引用 skill 挑刺，不发明新规则
alwaysApply: true
---

# 交付前对抗式审查（强制）

`src/views/**`、`src/components/**` UI 任务：**结束前以「驳回 PR」立场**对照 skill 审查（编码流程见 `spec-first-coding.mdc`）。细则只在 reference 里，本文件不重复。

## 五问（每项须引用 reference 名，禁止自创规则）

0. 是否按 **`arco-first.md`** 优先级实现？有无可用 Arco props 却套了多余壳层？共享 class 是否说明了 why Arco 不够？
0.1 是否符合 **`theme-contract.md`**：GI 单一 baseline/palette、无项目主题适配层、`global.css` 仅直接语义 alias / 密度 / VXE 增强？
0.2 无参考图的既有项目改造是否按 **`existing-project-modernization.md`** 提供 before/after 实测证据？
0.3 是否把 reference 中未实现的示例类名误当成共享 API？是否先 grep 定义再使用？
1. 筛选字段数 → Tier → DOM 结构是否符合 **`filter-layout.md`**？
2. Modal / Drawer 是否符合 **`modal.md`**？
3. 列表 zone / 表格 / 分页是否符合 **`list-page.md`** + **`table.md`**？
4. 按钮层级是否符合 **`actions.md`**（每作用域 1× primary）？
5. 共享类名是否先过 **`arco-first.md`** 再 grep **`global.css`**？有无自写等效 CSS？
6. 动作显隐 / 可点 / 只读 / 确认是否符合 **`feature-delivery-contract.md`**？
7. 请求 / 成功失败 / 刷新范围 / 重复提交是否符合 **`feature-delivery-contract.md`**？
8. 权限 / 数据范围 / 状态流转是否符合 **`permissions.md`** + **`feature-delivery-contract.md`**？
9. icon 的库选择 / 作用域 / 内容形式是否符合 **`icons.md`** + **`actions.md`**？
10. 如果任务来自截图/原型，是否先完成了 **`prototype-to-ui-contract.md`**，而不是直接按视觉抄 UI？
11. 如果任务来自截图/原型，输入是否先满足了 **`artifact-intake-template.md`** 的最低字段？

## 输出（有不通过项则先修再结束）

```
[审查] 通过 | 不通过
- 问题：<reference 名>
- 证据：<路径或结构>
- 修复：<按 reference 一句话>
```

须同时通过 `node scripts/check-spec.js`；脚本与审查互补，不互相替代。
