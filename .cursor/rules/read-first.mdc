---
description: 开发 Vue 页面时必须先读的文件
globs: ["src/views/**/*.vue", "src/components/**/*.vue"]
alwaysApply: true
---

# 开发前必读（按顺序）

1. `ui-skill/freight-arco-ui/SKILL.md` — 入口与架构（`npm run sync-ui-skill` 同步到 `.cursor/skills/`）
2. 按任务读 `references/`：`module-patterns.md`（结构槽位）· `domain-language.md`（中文用语）· `table.md` / `actions.md`（细则）
3. `src/styles/global.css` — Token 与类名事实源
4. 参考实现：`src/views/orderManage/saleOrder/`

**禁止不读 skill + global.css 直接写页面。**
