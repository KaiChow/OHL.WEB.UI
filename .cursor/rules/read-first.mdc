---
description: 开发 Vue 页面时必须先读的文件
globs: ["src/views/**/*.vue", "src/components/**/*.vue"]
alwaysApply: true
---

# 开发前必读文件（按顺序）

1. `src/styles/global.css` — 所有 Token、布局类、组件类均在此，先读再写
2. `src/views/orderManage/saleOrder/index.vue` — 最标准的列表页示例
3. `src/views/orderManage/saleOrder/components/SaleOrderDetailDrawer.vue` — 最标准的抽屉示例

**读完确认哪些类可以直接复用后，再开始写代码。禁止不读直接写。**
