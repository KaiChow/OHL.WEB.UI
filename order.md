# 货代后台系统 PRD：海运出口订单管理中心

## 1. 产品定位

### 1.1 产品名称

海运出口订单管理中心

### 1.2 产品目标

建设一个面向货代公司内部操作、客服、商务、财务、单证、海外代理协同使用的订单管理后台，用于统一管理海运出口订单从订舱、拖车、报关、仓库、装柜、提单、费用、文件、异常、节点跟踪到结算的完整业务流程。

系统核心目标：

1. 提升订单处理效率，减少业务人员在多个系统、Excel、邮件之间反复切换。
2. 让订单状态、节点、费用、文件、异常一屏可见。
3. 支持高密度表格查询与快速操作，适合业务人员每日长时间使用。
4. 建立标准化订单详情结构，减少信息遗漏。
5. 支持订单异常预警、费用未确认、文件缺失、节点超期等风险提醒。

---

## 2. 用户角色

### 2.1 操作人员

负责订单创建、订舱、拖车、报关、装柜、节点维护、异常处理。

### 2.2 客服人员

负责客户沟通、订单跟进、文件收集、状态反馈。

### 2.3 单证人员

负责提单资料、SI、VGM、AMS/ISF、补料、提单确认。

### 2.4 财务人员

负责应收、应付、费用确认、利润核算、开票、付款状态跟进。

### 2.5 业务主管

负责订单监控、异常审批、数据审核、操作效率分析。

### 2.6 海外代理

查看海外段节点、目的港费用、派送信息、异常反馈。

---

## 3. 页面范围

本 PRD 包含以下页面：

1. 海运出口订单列表页
2. 订单详情页
3. 新增 / 编辑订单页
4. 订单费用页
5. 文件管理页
6. 物流节点跟踪页
7. 异常管理页
8. 操作日志页
9. 批量导入 / 导出页
10. 审批与风险提醒页

## 3.1 功能交付契约（Feature Contract）

本 PRD 中的每一个业务功能（点击、请求、权限、数据变更、状态流转）在进入开发前必须具备**最小完整契约**，不得以一句功能描述直接开工。契约是实现前置条件，不是交付后的补文档。

### 契约必备字段

| 字段 | 含义 |
| --- | --- |
| 功能 ID | 项目内唯一，kebab-case，页面规格与代码契约通过它互相引用 |
| 操作角色 | 允许执行该功能的角色（见第 2 章） |
| 可见条件 | 什么情况下该功能入口出现（无权限时隐藏还是禁用必须分开定义） |
| 可用条件 | 入口可见但不可点的条件（如已有在途请求、记录被锁定） |
| 请求与幂等 | 触发的本地操作或 API、关键参数、pending 归属；重复提交是拦截、合并还是幂等 |
| 成功结果 | 什么关闭、什么保持打开、什么立即可见 |
| 失败结果与现场保留 | 错误显示在哪个表面、哪些已输入内容和上下文被保留、如何重试；业务拒绝与网络失败分开定义；权限拒绝不得按网络错误处理 |
| 刷新范围 | 成功后精确刷新哪些区域（当前行、队列计数、详情区、日志等），禁止无脑整页刷新 |

### 风险附加条款

满足以下条件的功能，契约中必须追加对应条款：

| 条件 | 必须补充 |
| --- | --- |
| 数据变更 / 异步任务 | 加载锁定、重复提交处理、业务拒绝与网络失败区分、操作留痕 |
| 危险 / 不可逆操作 | 前置条件、确认文案与影响范围、无权限表现、取消行为 |
| 表单 / 可编辑表格 | 字段来源、条件必填/只读规则、校验归属、脏数据与取消策略、失败保留 |
| 状态流转 | 合法 from/to 状态、中间态、过期版本（stale revision）处理、非法流转反馈 |
| 批量操作 | 选择范围、混合资格处理、成功/失败计数、失败行保留选中、重试范围 |
| 查询 / 重置 | 已生效值与草稿值区分、回车行为、重置目标、分页重置、空/错误行为 |

### 全局状态矩阵

每个页面必须可复现、可恢复地覆盖以下适用状态，缺失任一适用状态即为发布缺陷：

1. 加载中（骨架或 Loading，按钮防重复提交）
2. 空数据（区分「无数据」与「筛选无结果」）
3. 无权限（独立表现，不冒充网络错误）
4. 校验失败（错误定位到字段归属表面）
5. 业务拒绝（如非法状态流转、财务锁定）
6. 网络失败 / 慢请求（可重试，现场保留）
7. 超长数据（长文本、极值不破坏布局）
8. 重复提交（双击、重复点击被拦截或幂等）
9. 批量部分失败（失败行可识别、保留选中、可重试）

---

# 4. 页面一：海运出口订单列表页

## 4.1 页面目标

订单列表页是操作人员的主工作台，负责快速查询、筛选、查看、批量处理订单。页面需要支持高密度信息展示，并保持清晰的业务层级。

## 4.2 页面结构

页面整体采用 PC 后台高密度布局：

1. 顶部标题区
2. 状态快捷筛选区
3. 查询条件区
4. 表格工具栏
5. 订单表格区
6. 表格右上角分页区
7. 批量操作区
8. 右侧订单详情抽屉

## 4.3 顶部标题区

### 展示内容

左侧：

* 页面标题：海运出口订单
* 副标题：统一管理订舱、拖车、报关、提单、费用、节点与异常

右侧：

* 新增订单按钮
* 批量导入按钮
* 导出按钮
* 刷新按钮
* 页面配置按钮

## 4.4 状态快捷筛选区

状态快捷筛选用于业务人员快速进入常用工作视角。

### 状态卡片

1. 全部订单
2. 待订舱
3. 待放舱
4. 待拖车
5. 待报关
6. 待装柜
7. 已开船
8. 待补料
9. 待提单确认
10. 费用未确认
11. 文件缺失
12. 异常订单

### 卡片字段

每个状态卡片展示：

* 状态名称
* 订单数量
* 今日新增数量
* 超期数量
* 风险标记

### 交互规则

1. 点击状态卡片后，自动刷新表格数据。
2. 当前选中的状态卡片需要高亮。
3. 异常订单、费用未确认、文件缺失需要使用风险色提示。
4. 状态卡片不应过大，建议单行横向滚动或自动换行。
5. 状态卡片只作为快捷入口，不替代表格查询条件。
6. 订单总数只允许有两个归属：分页区的总数与状态卡片上的队列计数；表格工具栏、上下文文案不得重复展示总数、已选队列名或风险计数。

---

## 4.5 查询条件区

### 默认展示查询项

默认只展示**一行**高频定位字段，保证表格在首屏占据主要高度：

* 关键词（订单号 / 提单号 / 订舱号 / 箱号 统一入口）
* 客户名称
* 起运港
* 目的港

行尾固定操作：查询、重置、更多筛选、查询方案。该行不产生独立的操作带，不挤压表格主体。

### 高级筛选（右侧抽屉）

点击「更多筛选」打开右侧筛选抽屉，高级条件按业务分组展示，不默认铺满页面：

* 航线与单证：船公司、船名航次、提单号、订舱号
* 执行与归属：订单状态、操作人员、业务类型
* 船期：开船日期、截关日期、更新时间
* 风险：是否异常、文件状态、费用状态、是否超期

抽屉交互规则：

1. 每个业务分组展示本组已生效条件数，并支持只清除本组草稿值。
2. 抽屉内实时展示当前条件的匹配订单数，应用前即可预判结果。
3. 条件有草稿态与已生效态之分：未点应用不触发表格查询，取消则丢弃草稿。
4. 应用后刷新表格、队列计数与分页；抽屉关闭，列表保持在抽屉旁可见，不切换为顶部浮层。
5. 布尔类条件（是否异常、是否超期）使用直接的三态控件，不用下拉模拟。

### 查询按钮

查询区右侧固定展示：

* 查询（本作用域唯一的主按钮）
* 重置
* 更多筛选
* 保存查询方案

### 查询方案

用户可以保存常用查询方案，例如：

1. 我的待处理订单
2. 今日截关订单
3. 本周开船订单
4. 费用未确认订单
5. 文件缺失订单
6. 异常待处理订单

查询方案支持：

* 新增
* 重命名
* 删除
* 设为默认
* 拖拽排序

---

## 4.6 表格工具栏

表格工具栏位于查询区下方，表格上方。

左侧展示：

* 当前查询方案名称
* 已选择数量（勾选后出现）

结果总数不在工具栏重复展示，唯一归属为右上角分页与状态卡片计数。

右侧展示：

* 批量分配操作员
* 批量修改状态
* 批量导出
* 批量生成费用
* 批量发送通知
* 表格列配置
* 密度切换
* 全屏表格

---

## 4.7 订单表格字段

表格采用高密度模式，默认行高建议 40px - 44px。

### 固定列

左侧固定：

1. 选择框
2. 订单号
3. 订单状态

右侧固定：

1. 风险标记
2. 操作

### 默认展示列

| 字段   | 说明                  |
| ---- | ------------------- |
| 订单号  | 系统生成，点击进入详情         |
| 客户名称 | 委托客户                |
| 业务类型 | FCL / LCL / 空运 / 铁运 |
| 订单状态 | 当前主流程状态             |
| 船名航次 | Vessel / Voyage     |
| 起运港  | POL                 |
| 目的港  | POD                 |
| 开船日期 | ETD                 |
| 到港日期 | ETA                 |
| 截关时间 | Closing Time        |
| 提单号  | MBL / HBL           |
| 订舱号  | Booking No.         |
| 柜型柜量 | 例如 2*40HQ           |
| 操作人员 | 当前负责人               |
| 文件状态 | 完整 / 缺失 / 待确认       |
| 费用状态 | 未生成 / 待确认 / 已确认     |
| 异常状态 | 正常 / 异常 / 已处理       |
| 更新时间 | 最后更新时间              |

### 表格视觉要求

1. 表格以信息密度优先，不做大卡片化设计。
2. 重要字段如订单号、客户、状态、ETD、截关时间需要清晰突出。
3. 超期、异常、费用风险、文件缺失需要使用 Tag 标识。
4. 状态统一使用 `.s-pill[data-s]` 状态徽章呈现；禁止按状态给整行大面积着色，且状态不得只依赖颜色区分（文字 + 徽章色调同时表达）。
5. 鼠标悬浮行展示浅色背景。
6. 表格支持横向滚动。
7. 表头支持固定。
8. 分页必须放在表格右上角，符合业务方使用习惯。
9. 列表主表使用共享的 `workbench-table`（VXE）实现，禁止直接使用 `a-table`；密度切换、列固定、列配置、加载与空态均走组件公开配置，不写覆盖框架内部的页面级样式。
10. 业务操作控件统一显式 `size="small"`；同一个操作作用域内只允许一个 `type="primary"` 主按钮。

---

## 4.8 表格行操作

每行操作默认展示：

* 详情
* 编辑
* 更多

点击「更多」后展示：

1. 修改状态
2. 分配操作员
3. 生成费用
4. 上传文件
5. 发送客户通知
6. 标记异常
7. 查看日志
8. 复制订单
9. 作废订单

### 操作规则

1. 高频操作前置：行内默认直接可见的操作不超过 2 个（详情、编辑），其余全部收进「更多」。
2. 危险操作如作废订单必须二次确认，确认文案需说明影响范围。
3. 不同角色看到的操作按钮不同；无权限的入口按契约定义隐藏或禁用，两种状态不得混用。
4. 财务已锁定订单，不允许普通操作人员修改费用相关字段（入口禁用并说明原因）。
5. 已开船订单修改关键字段时，需要记录变更原因。
6. 每个行操作触发后必须有明确的结果反馈：成功精确刷新受影响行与队列计数；失败保留当前筛选、分页与选中现场，错误就近显示在可恢复的表面。
7. 批量操作必须区分全部成功、部分失败、全部失败；部分失败时失败行保留选中并给出可下载的失败明细。

---

# 5. 页面二：订单详情页

## 5.1 页面目标

订单详情页用于集中展示订单完整业务信息，支持查看、编辑、跟进、审批和协同。

## 5.2 页面结构

订单详情页采用：

1. 顶部订单摘要区
2. 左侧主内容区
3. 右侧风险与待办区
4. Tab 信息区
5. 底部操作栏

## 5.3 顶部订单摘要区

### 展示字段

* 订单号
* 订单状态
* 客户名称
* 业务类型
* 船名航次
* 起运港
* 目的港
* ETD
* ETA
* 当前操作员
* 异常状态
* 利润状态

### 快捷操作

* 编辑订单
* 修改状态
* 生成费用
* 上传文件
* 发送通知
* 标记异常
* 更多操作

## 5.4 右侧风险与待办区

展示当前订单需要优先处理的问题。

### 风险卡片

1. 截关时间不足 24 小时
2. 报关资料未上传
3. 提单资料待确认
4. 费用未确认
5. 应收未开票
6. 海外代理未反馈
7. 客户有未读消息
8. 存在异常未关闭

### 待办卡片字段

* 待办类型
* 负责人
* 截止时间
* 当前状态
* 快捷处理按钮

---

## 5.5 订单详情 Tab

详情页包含以下 Tab：

1. 基本信息
2. 订舱信息
3. 柜货信息
4. 拖车信息
5. 报关信息
6. 提单信息
7. 文件资料
8. 费用信息
9. 物流节点
10. 异常记录
11. 沟通记录
12. 操作日志

---

# 6. Tab 一：基本信息

## 6.1 展示内容

### 客户信息

* 客户名称
* 客户联系人
* 联系电话
* 邮箱
* 销售人员
* 客服人员

### 订单信息

* 订单号
* 业务类型
* 贸易条款
* 运输条款
* 付款方式
* 操作人员
* 创建时间
* 订单来源

### 航线信息

* 起运港
* 目的港
* 中转港
* 船公司
* 船名
* 航次
* ETD
* ETA
* 截关时间
* 截补料时间

## 6.2 功能

本 Tab 默认只读展示，点击进入编辑会话，保存/取消不丢失当前 Tab 上下文。功能契约如下：

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| order-basic-edit | 操作、主管 | 有订单编辑权限且订单未归档可见；有在途保存时不可用 | 携带订单 ID 与当前版本号提交变更；重复点击拦截 | 退出编辑态、立即展示新值、保留当前 Tab | 保持编辑态与全部已输入值；字段错误定位到对应分组，网络错误可重试；版本过期提示刷新后对比 | 详情摘要区、基本信息分组、列表对应行、操作日志 |
| order-basic-copy | 操作、主管 | 有创建权限可见 | 以当前订单为模板初始化草稿并跳转新建页 | 打开带预填值的新建草稿 | 停留在详情页并提示草稿初始化失败 | 无（保存成功后才刷新列表队列） |

业务规则：

1. 修改关键字段（客户、起运港、目的港、ETD、截关时间）必须填写修改原因并写入操作日志。
2. 修改 ETD、ETA、截关时间后自动重算相关节点提醒与风险规则。
3. 修改客户后，需要重新校验客户信用状态；信用异常时给出风险提示但不强制阻断。
4. 已完成、已取消订单默认为只读，编辑入口隐藏。

---

# 7. Tab 二：订舱信息

## 7.1 展示字段

* 订舱号
* 船公司
* 订舱代理
* 合约号
* 运价编号
* 舱位状态
* 放舱时间
* 截关时间
* 截补料时间
* 截 VGM 时间
* SO 文件
* 订舱备注

## 7.2 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| booking-save | 操作、主管 | 有订舱维护权限可见；订单未锁定可用 | 携带订单 ID 与版本号提交订舱表单；重复提交拦截 | 退出编辑态并展示新值 | 保持表单与已输入值，错误定位到字段，可重试 | 订舱 Tab、操作日志 |
| booking-so-upload | 操作、主管 | 订舱记录存在可见 | 上传 SO 文件，同一文件重复上传需确认覆盖或生成新版本 | 文件列表出现新版本，SO 缺失风险消除 | 保留其他已上传文件，失败条目就地提示可重传 | 文件清单、风险提醒 |
| booking-space-status-change | 操作、主管 | 舱位状态存在合法目标态可用 | 提交目标状态与原因；非法流转业务拒绝 | 状态徽章更新，同步推动订单主状态 | 保持弹窗与已选值，业务拒绝就近提示 | 订舱 Tab、订单主状态、物流节点、队列计数 |
| booking-fail-record | 操作、主管 | 舱位状态为订舱失败可见 | 提交失败原因 | 原因记录并通知相关人 | 保留输入，可重试 | 订舱 Tab、异常记录（如勾选转异常）、操作日志 |
| booking-rebook | 操作、主管 | 订舱失败或被取消后可见可用 | 以原订舱信息初始化新订舱记录 | 生成新订舱记录并进入编辑 | 保留原记录，失败提示可重试 | 订舱 Tab、操作日志 |

业务规则：

1. 放舱后自动生成后续待办：拖车、报关、补料、VGM，并进入对应负责人工作台。
2. 舱位状态变更同步订单主状态时，若主状态流转非法（如已完成订单），本次变更被拒绝并说明原因。
3. 放舱时间、截补料时间、截 VGM 时间变更后，自动重算风险提醒规则。

---

# 8. Tab 三：柜货信息

## 8.1 柜型柜量

支持录入：

* 20GP
* 40GP
* 40HQ
* 45HQ
* SOC / COC
* 冷柜
* 危险品柜

## 8.2 箱号封号

字段：

* 箱号
* 封号
* 柜型
* 皮重
* 毛重
* 件数
* 体积
* 装柜时间
* 装柜地点
* 备注

## 8.3 货物信息

字段：

* 品名
* HS Code
* 件数
* 包装
* 毛重
* 净重
* 体积
* 唛头
* 是否危险品
* 是否电池
* 是否木质包装
* 是否需要商检

## 8.4 功能

柜与货物明细使用子表（`detail-mini-vxe`）维护，行编辑就地完成，控件只出现在当前编辑行。功能契约如下：

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| cargo-container-save | 操作、主管 | 有柜货维护权限可见；无其他编辑中行可用 | 提交单行柜信息；箱号格式校验不通过则前端拒绝 | 行退出编辑态，汇总行重算 | 保持该行编辑态与已输入值，错误定位到单元格 | 柜子表、柜型柜量汇总、操作日志 |
| cargo-container-import | 操作、主管 | 有柜货维护权限可见 | 批量导入箱号封号（Excel 粘贴或文件），重复箱号按业务规则拦截或合并 | 展示导入成功/失败计数，失败行可下载 | 已录入数据保留，失败行就地标记原因 | 柜子表、异常提示 |
| cargo-goods-save | 操作、主管 | 柜记录存在可见 | 按柜提交货物信息 | 行退出编辑态，重量/体积汇总重算 | 保持编辑态与输入，可重试 | 货物子表、汇总行 |
| cargo-paste-excel | 操作、主管 | 货物子表可见 | 解析剪贴板数据并预览确认后写入 | 预览确认后批量落表 | 解析失败行在预览中标记，不写库 | 货物子表 |

业务规则：

1. 箱号格式按 ISO 6346 校验，格式错误前端拒绝并提示。
2. 自动汇总各柜重量、件数、体积，与订舱/提单数据不一致时给出差异提示。
3. 异常提示：箱号缺失、封号缺失、重量超限、危险品资料缺失；危险品柜必须上传危险品资料后才能标记装柜完成。

---

# 9. Tab 四：拖车信息

## 9.1 展示字段

* 拖车供应商
* 提柜地点
* 还柜地点
* 装柜地址
* 装柜时间
* 联系人
* 联系电话
* 司机姓名
* 司机电话
* 车牌号
* 箱号
* 拖车状态
* 拖车费用
* 派车单

## 9.2 拖车状态

1. 未安排
2. 已派车
3. 已提柜
4. 已到厂
5. 装柜中
6. 已离厂
7. 已还柜
8. 异常

## 9.3 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| trucking-save | 操作、主管 | 有拖车维护权限可见；已还柜记录只读 | 提交拖车安排表单；重复提交拦截 | 退出编辑态展示新值 | 保留表单与输入，错误定位字段，可重试 | 拖车 Tab、操作日志 |
| trucking-assign-vendor | 操作、主管 | 拖车状态为未安排可用 | 提交供应商与费用；携带版本号 | 供应商与费用落表，状态流转为已派车 | 版本过期提示刷新对比；业务拒绝就近提示 | 拖车 Tab、费用模块（自动带入）、物流节点 |
| trucking-dispatch-note | 操作、主管 | 已派车可见 | 生成派车单（幂等：重复生成返回同一单据） | 派车单可预览、下载 | 失败提示可重试，不影响已保存数据 | 拖车 Tab、文件清单 |
| trucking-receipt-upload | 操作、主管 | 派车单存在可见 | 上传派车回单 | 回单出现在文件清单 | 失败就地提示可重传 | 文件清单、拖车 Tab |
| trucking-node-change | 操作、主管 | 存在合法目标节点可用 | 提交目标节点与时间；非法流转业务拒绝 | 节点更新并同步物流节点时间轴 | 保留已选值，业务拒绝说明原因 | 拖车 Tab、物流节点、订单主状态 |
| trucking-exception-report | 操作、主管、客服 | 拖车记录存在可见 | 创建拖车异常并关联责任人 | 异常记录生成，订单出现风险标记 | 保留异常表单输入，可重试 | 异常记录、风险提醒、操作日志 |

业务规则：

1. 拖车费用确认后自动带入费用模块应付费用，来源标记为拖车，禁止重复生成。
2. 拖车节点变更后自动同步物流节点时间轴，并写入操作日志。
3. 司机姓名、电话、车牌号在派车前必填，缺失时「已派车」流转被业务拒绝。

---

# 10. Tab 五：报关信息

## 10.1 展示字段

* 报关方式
* 报关行
* 报关单号
* 报关状态
* 截关时间
* 放行时间
* 查验状态
* 查验结果
* 报关资料
* 报关备注

## 10.2 报关状态

1. 未提交
2. 资料待补齐
3. 已提交
4. 查验中
5. 已放行
6. 退单
7. 异常

## 10.3 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| customs-doc-upload | 操作、单证 | 有报关维护权限可见 | 上传报关资料，按文件分类归档 | 文件清单更新，资料缺失风险消除 | 失败条目就地提示可重传，已上传文件保留 | 文件清单、报关 Tab、风险提醒 |
| customs-status-change | 操作、主管 | 存在合法目标状态可用 | 提交目标状态、时间与原因；非法流转业务拒绝 | 状态徽章更新；放行时同步订单节点 | 保留已选值；业务拒绝就近说明原因 | 报关 Tab、物流节点、订单主状态 |
| customs-inspection-record | 操作、主管 | 报关状态为查验中可见 | 提交查验信息（查验状态、结果） | 查验记录落表 | 保留输入，可重试 | 报关 Tab、操作日志 |
| customs-reject-record | 操作、主管 | 报关被退单时可用 | 提交退单原因，自动生成异常（可选） | 退单原因落表并通知负责人 | 保留输入，可重试 | 报关 Tab、异常记录、风险提醒 |

业务规则：

1. 报关放行后自动更新订单物流节点与主状态，并写入操作日志。
2. 报关异常自动进入异常管理，异常等级不低于中。
3. 截关前 24 小时资料未齐，系统自动触发风险提醒并进入相关人待办。
4. 报关费用确认后自动带入费用模块（应收/应付按委托关系区分），来源标记为报关。

---

# 11. Tab 六：提单信息

## 11.1 展示字段

* MBL No.
* HBL No.
* Shipper
* Consignee
* Notify Party
* Description of Goods
* Marks & Numbers
* 件数
* 毛重
* 体积
* 提单类型
* 放单方式
* SI 状态
* 补料状态
* 提单确认状态

## 11.2 提单状态

1. 待补料
2. 已补料
3. 船司草单已出
4. 客户待确认
5. 客户已确认
6. 正本已出
7. 电放完成
8. Sea Waybill 完成

## 11.3 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| bl-info-save | 单证、操作、主管 | 有提单维护权限可见；提单已确认后关键字段锁定 | 提交提单资料；完整性校验不通过则业务拒绝并列出缺失字段 | 退出编辑态展示新值，补料状态自动推进 | 保留全部输入，缺失字段逐项标注 | 提单 Tab、风险提醒、操作日志 |
| bl-si-upload | 单证、操作 | 提单记录存在可见 | 上传 SI 文件 | SI 状态更新，截补料风险消除 | 失败就地提示可重传 | 文件清单、提单 Tab |
| bl-preview | 单证、操作、客服 | 提单必填字段完整可用 | 生成提单预览（只读，不落库） | 打开预览浮层 | 字段不完整时列出缺失项 | 无 |
| bl-send-confirm | 单证、操作 | 草单已出可用 | 发送客户确认请求（幂等：重复发送提示已在途） | 状态流转为客户待确认，发送记录留痕 | 失败保留当前状态并提示重试 | 提单 Tab、沟通记录 |
| bl-customer-confirm | 客户（门户）、单证 | 状态为客户待确认可用 | 提交确认结果 | 状态流转为客户已确认，关键字段锁定 | 失败可重试，状态不变 | 提单 Tab、物流节点 |
| bl-confirmed-edit | 单证、主管 | 提单已确认可见；需提交修改审批可用 | 携带修改原因提交审批，审批通过后生效 | 进入审批中状态，审批通过后更新并留痕 | 审批被拒绝时保留原值并通知发起人 | 提单 Tab、审批列表、操作日志 |
| bl-download | 单证、操作、客服、财务 | 有提单查看权限可见 | 下载 HBL / MBL 文件 | 文件下载完成 | 失败提示重试 | 无 |

业务规则：

1. 自动校验提单字段完整性（Shipper、Consignee、Notify、品名、件毛体），缺失时禁止提交补料。
2. Shipper / Consignee / Notify / Goods 支持按 35 字符换行格式化预览。
3. 客户确认后锁定提单关键字段；修改已确认提单必须走审批并记录原因。

---

# 12. Tab 七：文件资料

## 12.1 文件分类

1. 订舱文件
2. SO 文件
3. 报关资料
4. 装箱单
5. 发票
6. 合同
7. 提单文件
8. 拖车文件
9. 仓库文件
10. 费用文件
11. 客户邮件附件
12. 其他文件

## 12.2 文件字段

* 文件名称
* 文件类型
* 文件分类
* 上传人
* 上传时间
* 文件状态
* 是否必填
* 是否已确认
* 备注

## 12.3 功能

文件列表使用子表维护，上传进度、失败重传、版本历史均在列表内就近反馈。功能契约如下：

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| file-upload | 操作、单证、客服、海外代理（限海外段） | 有上传权限可见 | 单个或批量上传，带分类与备注；同名文件提示覆盖或新版本 | 文件入列，缺失提醒自动消除 | 失败条目就地标记可重传，其余上传不受影响 | 文件清单、文件状态、风险提醒 |
| file-preview | 全部有查看权限角色 | 文件存在可用 | 打开预览浮层（图片/PDF 在线预览） | 预览打开 | 不支持格式提示改为下载 | 无 |
| file-download | 全部有查看权限角色 | 文件存在可用 | 下载文件 | 下载完成 | 失败提示重试 | 无 |
| file-rename | 操作、单证 | 文件未锁定可用 | 提交新名称；重名校验 | 名称更新 | 保留输入，冲突提示 | 文件清单 |
| file-recategorize | 操作、单证 | 文件未锁定可用 | 提交新分类 | 分类更新，缺失规则重算 | 失败提示重试 | 文件清单、文件状态 |
| file-version-history | 全部有查看权限角色 | 文件存在多版本可用 | 查看并下载历史版本 | 版本列表打开 | 失败提示重试 | 无 |
| file-confirm / file-reject | 单证、主管、财务（费用文件） | 文件状态为待确认可用 | 提交确认或驳回（驳回必填原因） | 状态更新并通知上传人 | 保留原因输入，可重试 | 文件清单、文件状态、消息通知 |
| file-delete | 操作、单证、主管 | 文件未锁定且未被业务引用可用；已确认文件仅主管 | 删除前二次确认并说明影响 | 文件移除，缺失规则重算 | 删除被拒绝（被引用）时说明引用来源 | 文件清单、文件状态、风险提醒 |

业务规则：

1. 必填文件缺失时，文件状态列与详情风险区同步提示，并进入负责人待办。
2. 文件删除为危险操作，必须二次确认；被费用、提单等业务引用的文件禁止删除。
3. 海外代理仅能上传与查看海外段文件分类，其余分类不可见。
4. 文件上传、删除、确认、驳回均自动写入操作日志。

## 12.4 文件缺失规则

以下情况需要提示文件缺失：

1. 已放舱但未上传 SO。
2. 截关前 24 小时未上传报关资料。
3. 已装柜但未上传装箱单。
4. 已补料但无提单确认文件。
5. 已产生费用但无供应商账单附件。

---

# 13. Tab 八：费用信息

## 13.1 页面目标

费用信息页用于管理订单应收、应付、利润、开票、付款状态。

## 13.2 页面结构

费用页分为：

1. 利润概览
2. 应收费用表
3. 应付费用表
4. 费用差异提醒
5. 开票与付款状态

## 13.3 利润概览

展示：

* 应收合计
* 应付合计
* 毛利
* 毛利率
* 未确认应收
* 未确认应付
* 已开票金额
* 已付款金额
* 未收款金额
* 未付款金额

## 13.4 应收费用字段

* 费用名称
* 币种
* 单价
* 数量
* 单位
* 金额
* 税率
* 含税金额
* 客户
* 费用状态
* 开票状态
* 收款状态
* 备注

## 13.5 应付费用字段

* 费用名称
* 币种
* 单价
* 数量
* 单位
* 金额
* 税率
* 含税金额
* 供应商
* 费用状态
* 付款状态
* 账单附件
* 备注

## 13.6 功能

应收、应付两张费用表独立维护，利润概览的数字只能由费用表汇总产生，不允许手工编辑。功能契约如下：

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| fee-receivable-save / fee-payable-save | 操作、财务 | 有费用维护权限可见；财务锁定后操作角色不可用 | 提交费用行；币种、金额、数量校验 | 行退出编辑态，利润概览重算 | 保持编辑行与输入，错误定位单元格 | 费用表、利润概览、操作日志 |
| fee-import | 财务、操作 | 有费用维护权限可见 | 批量导入费用，模板校验后预览确认 | 展示成功/失败计数，失败行可下载 | 已录入费用保留，失败行就地标记原因 | 费用表、利润概览 |
| fee-from-quote | 操作、财务 | 订单有关联报价可用 | 从报价带入费用（幂等：重复带入提示已存在） | 费用行生成并标记来源 | 无报价时引导先维护报价 | 费用表、利润概览 |
| fee-auto-generate | 系统 | 拖车/报关/仓库节点完成触发 | 按来源模块自动生成费用，来源唯一约束防重复 | 费用行生成并标记来源模块 | 生成失败进入异常提醒 | 费用表、利润概览、风险提醒 |
| fee-confirm / fee-reject | 财务、主管 | 费用状态为待确认可用 | 提交确认或驳回（驳回必填原因） | 状态更新，未确认计数减少 | 保留原因输入，可重试 | 费用表、费用状态、消息通知 |
| fee-confirmed-modify-approval | 财务、主管 | 费用已确认可见 | 携带修改原因提交审批，通过后生效 | 进入审批中状态 | 审批被拒保留原值并通知发起人 | 费用表、审批列表、操作日志 |
| fee-export | 财务、操作、主管 | 有费用查看权限可见 | 导出当前订单费用明细 | 文件下载完成 | 失败提示重试 | 无 |
| fee-bill-generate | 财务 | 存在已确认应收可用 | 生成账单（幂等：同一批次重复生成返回同一账单） | 账单生成并可下载 | 失败提示重试，费用状态不变 | 费用表、文件清单 |
| fee-invoice-apply / fee-payment-apply | 财务 | 应收已确认 / 应付已确认可用 | 提交开票/付款申请 | 申请进入审批流，状态更新 | 保留表单输入，可重试 | 费用表、开票/付款状态、审批列表 |

## 13.7 财务控制规则

1. 已确认费用不允许普通操作人员直接修改，入口禁用并说明锁定原因。
2. 已开票费用不允许删除，删除入口隐藏。
3. 已付款费用不允许修改金额，金额列只读。
4. 修改已确认费用必须提交审批，审批通过前原值继续生效。
5. 毛利率低于系统阈值时，利润概览给出风险提示，继续提交需主管确认。
6. 应付大于应收时，订单标记为亏损订单并进入风险提醒。
7. 利润、毛利率等敏感字段按字段权限控制，无权限角色不渲染而非置空。

---

# 14. Tab 九：物流节点

## 14.1 节点展示方式

物流节点采用时间轴展示，并支持表格模式切换。

## 14.2 默认节点

1. 订单创建
2. 订舱提交
3. 船司放舱
4. 安排拖车
5. 提柜
6. 到厂
7. 装柜完成
8. 还柜
9. 报关提交
10. 报关放行
11. 截关
12. 开船
13. 到港
14. 清关
15. 派送
16. 签收
17. 订单完成

## 14.3 节点字段

* 节点名称
* 计划时间
* 实际时间
* 节点状态
* 负责人
* 来源模块
* 备注
* 附件
* 是否超期

## 14.4 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| node-save | 操作、主管、海外代理（限海外段） | 有节点维护权限可见 | 新增或编辑节点计划/实际时间；重复提交拦截 | 时间轴与表格模式同步更新 | 保留表单输入，错误定位字段 | 物流节点、操作日志 |
| node-complete | 操作、主管、海外代理（限海外段） | 节点未完成且前置节点满足可用 | 标记完成并记录实际时间 | 节点状态更新，超期提醒消除 | 前置不满足时业务拒绝并说明缺失节点 | 物流节点、订单主状态、风险提醒 |
| node-exception-report | 操作、客服、主管 | 节点存在可见 | 创建节点异常并关联责任人 | 异常生成，节点显示异常标记 | 保留异常表单输入 | 异常记录、物流节点、风险提醒 |
| node-visibility-toggle | 操作、主管 | 有节点维护权限可用 | 切换节点对客户可见/内部可见 | 可见性标记更新 | 失败提示重试 | 物流节点、客户门户 |
| node-notify | 操作、客服 | 节点已发生变更可用 | 向客户/相关人发送节点更新通知（幂等防重发） | 通知记录写入沟通记录 | 发送失败保留重试入口 | 沟通记录、消息通知 |

业务规则：

1. 节点完成后自动推动订单主状态；若主状态流转非法，节点完成被拒绝并说明原因。
2. 计划时间已过而未完成的节点自动标记超期并进入风险提醒。
3. 节点新增、时间修改、完成、可见性变更均自动写入操作日志。
4. 海外代理仅能维护海外段节点（到港、清关、派送、签收），其余节点只读或不可见。
5. 时间轴与表格模式是同一数据的两种视图，计数与状态不得出现两个不同值。

---

# 15. Tab 十：异常记录

## 15.1 异常类型

1. 订舱异常
2. 拖车异常
3. 报关异常
4. 文件异常
5. 费用异常
6. 客户异常
7. 船期延误
8. 柜号异常
9. 查验异常
10. 海外段异常

## 15.2 异常字段

* 异常编号
* 异常类型
* 异常等级
* 异常描述
* 责任部门
* 责任人
* 发生时间
* 期望解决时间
* 当前状态
* 处理方案
* 处理结果
* 附件
* 关闭时间

## 15.3 异常等级

1. 低：不影响订单正常流转
2. 中：影响部分节点，需要跟进
3. 高：影响截关、开船、放货、费用
4. 严重：可能造成客户投诉、罚款、亏损

## 15.4 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| exception-create | 操作、客服、单证、财务、主管 | 有异常上报权限可见 | 提交异常表单（类型、等级、描述、责任人、期望解决时间）；重复提交拦截 | 异常入列，订单主状态出现风险标记，责任人收到待办 | 保留全部表单输入，错误定位字段，可重试 | 异常记录、风险提醒、消息通知 |
| exception-assign | 主管、操作 | 异常未关闭可用 | 提交新责任人 | 责任人更新并收到待办 | 失败提示重试 | 异常记录、消息通知 |
| exception-attachment-upload | 相关角色 | 异常未关闭可用 | 上传异常附件 | 附件入列 | 失败就地提示可重传 | 异常记录、文件清单 |
| exception-progress-record | 责任人、主管 | 异常处理中可用 | 追加处理过程记录 | 处理时间线更新 | 保留输入，可重试 | 异常记录、操作日志 |
| exception-status-change | 责任人、主管 | 存在合法目标状态可用 | 提交目标状态；非法流转业务拒绝 | 状态徽章更新 | 保留已选值，业务拒绝说明原因 | 异常记录、订单风险标记 |
| exception-close | 责任人、主管 | 处理结果已填写可用；高等级异常需主管确认 | 提交关闭（处理结果必填，缺失则业务拒绝） | 异常关闭并写入订单日志，风险标记消除 | 缺失处理结果时定位到该字段 | 异常记录、风险提醒、操作日志 |

业务规则：

1. 异常关闭必须填写处理结果，否则关闭被业务拒绝。
2. 高等级、严重等级异常的关闭需要主管确认。
3. 存在未关闭异常时，订单主状态持续展示风险标记；全部关闭后自动消除。
4. 异常创建、指派、状态变更、关闭均自动写入操作日志；高等级异常超过 24 小时未处理自动升级提醒。

---

# 16. Tab 十一：沟通记录

## 16.1 沟通类型

1. 客户沟通
2. 船公司沟通
3. 供应商沟通
4. 海外代理沟通
5. 内部沟通
6. 系统通知

## 16.2 展示字段

* 沟通时间
* 沟通对象
* 沟通方式
* 沟通内容
* 关联文件
* 记录人
* 是否客户可见
* 是否需要跟进

## 16.3 功能

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| comm-record-create | 操作、客服、单证、主管 | 有沟通记录权限可见 | 提交沟通记录（对象、方式、内容、是否客户可见）；重复提交拦截 | 记录入列，需跟进的生成提醒 | 保留全部输入，可重试 | 沟通记录、待办提醒 |
| comm-link-file | 操作、客服 | 沟通记录存在可用 | 关联订单内文件 | 关联文件出现在记录上 | 失败提示重试 | 沟通记录 |
| comm-follow-up-done | 记录人、客服 | 记录标记为需要跟进可用 | 标记已跟进 | 跟进提醒消除 | 失败提示重试 | 沟通记录、待办提醒 |
| comm-customer-notify | 操作、客服 | 订单存在客户联系人可用 | 发送客户状态通知（幂等防重发），仅发送客户可见内容 | 发送记录写入沟通记录 | 发送失败保留重试入口 | 沟通记录、消息通知 |

业务规则：

1. 内部备注与客户可见内容必须分字段存储、分开展示，客户门户只输出客户可见内容。
2. 从邮件同步沟通记录为系统行为，同步失败进入异常提醒，不阻塞手工记录。
3. 需跟进的沟通记录逾期未完成时，自动进入记录人与客服待办。

---

# 17. Tab 十二：操作日志

## 17.1 日志字段

* 操作时间
* 操作人
* 操作模块
* 操作类型
* 变更前内容
* 变更后内容
* 操作来源
* IP 地址
* 备注

## 17.2 功能

操作日志以系统自动记录为主，不提供手工新增、编辑、删除入口。功能契约如下：

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| log-auto-record | 系统 | 订单关键字段变更、费用修改、文件上传/删除/确认、状态流转、异常处理时触发 | 系统行为，携带操作人、来源、版本号；失败重试不阻塞业务操作 | 日志追加，详情页日志 Tab 计数更新 | 记录失败进入系统告警，业务操作本身不回滚 | 操作日志 |
| log-filter | 全部有查看权限角色 | 日志 Tab 可见 | 按操作人、时间范围、模块筛选 | 列表刷新 | 失败保留筛选条件并可重试 | 操作日志列表 |
| log-export | 主管、财务、操作 | 有日志查看权限可见 | 导出当前筛选结果 | 文件下载完成 | 失败提示重试 | 无 |

业务规则：

1. 日志为只读事实流，任何角色不得修改或删除。
2. 变更前后内容需以可对比方式展示（字段名 + 旧值 + 新值），敏感字段按字段权限脱敏。
3. 海外代理不可见内部操作日志，仅可见与其相关的节点与文件日志。

---

# 18. 页面三：新增 / 编辑订单页

## 18.1 页面目标

支持业务人员快速创建海运出口订单，同时保证核心字段完整、业务流程可继续流转。

## 18.2 页面结构

采用分组表单：

1. 客户信息
2. 航线信息
3. 货物信息
4. 柜型柜量
5. 订舱信息
6. 服务项目
7. 备注信息

## 18.3 必填字段

* 客户名称
* 业务类型
* 起运港
* 目的港
* ETD
* 柜型柜量
* 操作人员
* 服务项目

## 18.4 服务项目

支持勾选：

1. 订舱
2. 拖车
3. 报关
4. 仓库
5. 提单
6. 保险
7. 目的港清关
8. 目的港派送

## 18.5 表单交互

1. 客户选择后自动带出联系人、付款方式、信用状态；信用异常时展示风险提示，不强制阻断。
2. 起运港、目的港选择后自动匹配常用航线。
3. 选择服务项目后，详情页自动生成对应业务 Tab 和待办。
4. 柜型柜量支持动态增减。
5. 表单按业务分组展示，校验错误定位到所属分组与字段，不只用顶部汇总提示。
6. 离开页面存在未保存内容时，必须二次确认（保存草稿 / 放弃 / 继续编辑）。

### 提交契约

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| order-draft-save | 操作、主管 | 有创建权限可见；无在途提交可用 | 只校验基础字段，保存草稿；重复点击拦截 | 提示已保存，停留在当前表单 | 失败保留全部输入，可重试 | 草稿状态标记 |
| order-submit | 操作、主管 | 全部必填字段通过校验可用 | 校验全部必填字段，携带幂等键提交，防止网络重试产生重复订单 | 跳转订单详情页，草稿转为正式订单 | 校验失败定位到分组与字段；网络失败保留全部输入可重试；业务拒绝（如客户信用冻结）说明原因 | 列表队列计数、订单列表首页 |

业务规则：

1. 保存草稿只校验基础字段（客户、业务类型），提交订单校验全部必填字段。
2. 创建成功后跳转订单详情页，并保留返回列表时的筛选与分页上下文。
3. 提交成功后列表页「全部订单 / 待订舱」队列计数与首页数据在返回时刷新。

---

# 19. 页面四：批量导入 / 导出页

## 19.1 批量导入

支持导入：

1. 订单基础信息
2. 箱号封号
3. 费用明细
4. 物流节点
5. 文件清单

## 19.2 导入流程

1. 下载模板
2. 上传 Excel
3. 系统解析
4. 数据预览
5. 错误校验
6. 确认导入
7. 导入结果反馈

## 19.3 错误校验

需要校验：

* 必填字段为空
* 日期格式错误
* 客户不存在
* 港口不存在
* 费用币种错误
* 箱号格式错误
* 订单号重复
* 数据权限不足

## 19.4 导出功能

支持导出：

1. 当前筛选结果
2. 选中订单
3. 订单明细
4. 费用明细
5. 文件清单
6. 操作日志

导出遵守数据权限与字段权限：无权限的订单与敏感字段不得出现在导出文件中。

## 19.5 导入导出契约

| 功能 ID | 操作角色 | 可见/可用条件 | 请求与幂等 | 成功结果 | 失败结果与现场保留 | 刷新范围 |
| --- | --- | --- | --- | --- | --- | --- |
| order-import | 操作、主管 | 有批量导入权限可见；无在途导入可用 | 上传 Excel 并逐行校验；确认导入携带幂等键，重复提交返回同一批结果 | 展示成功/失败计数，成功行落库，失败行生成可下载明细 | 校验阶段：错误行就地标记原因，不写库；导入阶段部分失败：成功行保留，失败行可修正后仅重试失败部分 | 订单列表、队列计数、操作日志 |
| order-export | 按导出类型对应角色 | 当前有数据可导出可用 | 生成导出文件（大数据量异步生成，完成后通知下载） | 文件下载完成，导出记录留痕 | 失败提示重试，不阻塞其他操作 | 无 |

业务规则：

1. 导入预览必须展示总行数、可导入行数、错误行数，错误行标注具体字段与原因。
2. 确认导入后禁止中途重复提交；部分失败时必须保留失败明细下载入口直到用户确认或会话结束。
3. 导出为异步任务时，生成期间入口展示进度状态，完成后通过消息通知。

---

# 20. 审批与权限

## 20.1 审批场景

以下操作需要审批：

1. 修改已确认费用
2. 删除已开票费用
3. 修改已确认提单
4. 作废订单
5. 关闭高等级异常
6. 修改已开船订单关键字段
7. 毛利率低于阈值继续提交

## 20.2 权限控制

### 操作人员

* 可创建订单
* 可编辑未锁定订单
* 可维护节点
* 可上传文件
* 可新增异常

### 财务人员

* 可维护费用
* 可确认应收应付
* 可处理开票付款
* 可查看利润

### 主管

* 可审批异常
* 可审批费用修改
* 可作废订单
* 可查看全部订单

### 海外代理

* 只能查看授权订单
* 只能维护海外段节点
* 只能上传海外段文件
* 不可查看内部利润

---

# 21. 状态流转

## 21.1 主订单状态

1. 草稿
2. 待订舱
3. 订舱中
4. 已放舱
5. 待拖车
6. 拖车中
7. 待报关
8. 报关中
9. 已放行
10. 待开船
11. 已开船
12. 运输中
13. 已到港
14. 清关中
15. 派送中
16. 已签收
17. 已完成
18. 已取消

## 21.2 状态规则

1. 状态可以由节点自动推动。
2. 状态也可以由有权限用户手动修改。
3. 手动修改状态必须填写原因。
4. 取消订单必须二次确认。
5. 已完成订单默认只读。
6. 财务未结清订单不能归档。

## 21.3 合法流转矩阵

状态流转只允许沿主流程推进或显式回退，禁止跨级跳转。合法流转如下（`→` 为允许方向）：

* 草稿 → 待订舱
* 待订舱 → 订舱中
* 订舱中 → 已放舱
* 已放舱 → 待拖车 / 待报关（服务项目决定分支）
* 待拖车 → 拖车中 → 待报关
* 待报关 → 报关中 → 已放行
* 已放行 → 待开船 → 已开船
* 已开船 → 运输中 → 已到港
* 已到港 → 清关中 → 派送中 → 已签收
* 已签收 / 已到港（无派送服务）→ 已完成
* 任意进行中状态 → 已取消（需二次确认并满足取消前置条件）

### 流转契约规则

1. 非法流转（跨级、逆向、已完成后再变更）被业务拒绝，并在操作入口就近说明当前允许的下一步。
2. 每次流转携带当前版本号；版本过期（他人已变更）时提示刷新对比，禁止静默覆盖。
3. 流转弹窗必须展示：当前状态、目标状态、修改原因、是否同步通知、是否生成节点记录。
4. 流转失败保留弹窗与已填原因；成功后精确刷新当前记录、受影响队列计数、列表行与操作日志。
5. 取消订单前置条件：无已确认应收未冲销、无未关闭高等级异常；不满足时给出具体阻断原因。

---

# 22. 风险提醒规则

系统需要自动识别以下风险：

1. 截关前 24 小时报关资料未齐。
2. 截补料前 12 小时 SI 未提交。
3. ETD 已过但订单未开船。
4. ETA 已过但未到港。
5. 已装柜但箱号封号缺失。
6. 已开船但提单未确认。
7. 应付费用大于应收费用。
8. 毛利率低于公司阈值。
9. 高等级异常超过 24 小时未处理。
10. 订单超过 7 天无任何更新。
11. 客户信用异常但仍创建订单。
12. 必填文件缺失。

---

# 23. 消息通知

## 23.1 通知类型

1. 系统提醒
2. 订单状态变更
3. 文件缺失提醒
4. 费用确认提醒
5. 异常处理提醒
6. 审批提醒
7. 客户通知
8. 海外代理通知

## 23.2 通知渠道

1. 系统站内信
2. 工作台待办
3. 邮件
4. 企业微信 / 钉钉
5. 客户门户消息

## 23.3 通知规则

1. 重要风险需要同时进入待办和消息中心。
2. 普通节点更新只进入订单沟通记录。
3. 审批通知必须推送给对应审批人。
4. 客户通知需要区分是否允许客户可见。
5. 通知发送后需要记录日志。

---

# 24. UI 设计要求

## 24.1 总体风格

系统风格要求：

* 专业
* 高效
* 结构化
* 高密度
* 高级感
* 业务优先
* 信息清晰
* 操作克制

## 24.2 适配范围

主要适配 PC 后台系统：

* 1920px 大屏
* 1440px 常规办公屏
* 1366px 笔记本屏
* 最小支持宽度 1280px

## 24.3 页面密度

1. 查询区不应占用过多高度。
2. 表格是页面主内容，占比应优先保证。
3. 详情页应减少无效留白。
4. 表单分组清晰，但不应做成过大的卡片。
5. 常用按钮前置，不常用按钮收纳。
6. 复杂筛选进入更多筛选面板，不默认铺满页面。

## 24.4 组件规范

组件使用遵循实现约束，不得以下面列表之外的方式自建替代品：

* 列表主表：共享 `workbench-table`（基于 `vxe-table`），**禁止使用 `a-table`**。
* 详情子表：共享 `detail-mini-vxe`，按行任务选择可编辑/只读密度。
* 状态呈现：统一 `.s-pill[data-s]` 状态徽章，禁止整行按状态着色、禁止只靠颜色区分状态。
* 业务控件：显式 `size="small"`；每个操作作用域只允许一个 `type="primary"`。
* 主题：GI 拥有 Arco 基线与色板，不新增主题适配层或页面级组件皮肤；全局样式不得改写 `.arco-*` / `.vxe-*` 内部结构。

Arco Design Vue 组件（表单与浮层类）：

* Form、Input、Select、DatePicker
* Drawer、Modal、Tabs、Tag、Badge、Timeline
* Descriptions、Upload、Dropdown、Popconfirm
* Message、Notification、Tooltip、Split、Card

## 24.5 表格规范

1. 表格支持列固定。
2. 支持列宽拖拽。
3. 支持列显隐配置（至少保留 8 个业务列，配置按用户持久化）。
4. 支持排序。
5. 支持筛选。
6. 支持多选。
7. 支持批量操作。
8. 支持空状态。
9. 支持加载状态。
10. 支持错误状态（表格内就近展示，可重试）。
11. 支持横向滚动。
12. 分页放在表格右上角。
13. 支持密度切换（标准行保留辅助决策信息行，紧凑行单行呈现）。
14. 行操作默认直接可见不超过 2 个，低频与危险操作收纳进「更多」。
15. 图标操作必须带 Tooltip 与业务化 `aria-label`。
16. 长文本（客户名、船名航次、超长单号）截断并保留悬停完整值，不得破坏行高与布局。

## 24.6 可访问性与视口验证

1. 键盘可按工作流顺序到达每个操作、行操作与打开的浮层，焦点可见、无焦点陷阱。
2. 所有仅图标命令有业务语义的可访问名称。
3. 200% 缩放、1366px 与 1280px 发布视口、1024px 分屏、1920px 宽屏下主工作与恢复路径保持可用。

---

# 25. 关键交互

## 25.1 快速查看详情

用户点击订单号后，不直接跳转页面，而是优先打开右侧详情抽屉。

抽屉展示：

* 订单摘要
* 当前状态
* 风险提醒
* 最近节点
* 费用概览
* 文件状态
* 最近日志
* 快捷操作

用户可点击「进入完整详情」跳转详情页。

## 25.2 批量操作

用户勾选多个订单后，表格工具栏展示批量操作：

* 批量分配操作员
* 批量修改状态
* 批量导出
* 批量发送通知
* 批量生成费用
* 批量标记异常

批量操作必须展示操作结果：

* 成功数量
* 失败数量
* 失败原因
* 可下载失败明细

批量操作契约规则：

1. 提交携带幂等键，重复点击拦截；成功行清除选中，失败行保留选中以便修正后仅重试失败部分。
2. 混合选择（部分订单不满足前置条件）时，执行前提示将跳过不符合项及原因，由用户确认继续。
3. 结果反馈就近展示在表格区域，失败明细在会话内保持可下载。

## 25.3 修改状态

修改状态时，需要弹窗确认。

弹窗字段：

* 当前状态
* 目标状态
* 修改原因
* 是否同步通知相关人员
* 是否生成节点记录

## 25.4 标记异常

标记异常时打开异常弹窗。

字段：

* 异常类型
* 异常等级
* 异常描述
* 责任人
* 期望解决时间
* 附件
* 是否通知主管

---

# 26. 数据权限

## 26.1 数据范围

订单数据根据用户角色控制：

1. 普通操作员：只能看自己负责订单。
2. 客服：可看自己客户订单。
3. 财务：可看费用相关订单。
4. 主管：可看所属部门订单。
5. 管理员：可看全部订单。
6. 海外代理：只能看授权订单。

## 26.2 字段权限

敏感字段包括：

* 利润
* 毛利率
* 应收
* 应付
* 客户信用
* 内部备注
* 成本供应商
* 财务审批记录

不同角色需要控制是否可见、是否可编辑。

---

# 27. 异常状态与空状态

## 27.1 空状态

表格无数据时展示：

* 暂无订单数据
* 当前筛选条件下无结果
* 可点击「重置筛选」
* 可点击「新增订单」

## 27.2 加载状态

数据加载时：

* 表格展示 Skeleton 或 Loading
* 按钮进入 loading 状态
* 禁止重复提交

## 27.3 错误状态

接口异常时展示：

* 错误提示
* 重试按钮
* 错误编号
* 联系管理员入口

## 27.4 权限不足

无权限时展示：

* 当前账号无权限查看该内容
* 可联系管理员申请权限

---

# 28. 验收标准

## 28.1 页面验收

1. 订单列表页可以正常展示订单数据。
2. 查询条件可以正常筛选数据。
3. 状态快捷筛选可以切换订单列表。
4. 表格支持横向滚动、列固定、分页、批量选择。
5. 分页展示在表格右上角。
6. 点击订单号可以打开详情抽屉。
7. 可以进入完整订单详情页。
8. 详情页 Tab 信息结构完整。
9. 费用、文件、节点、异常、日志模块均可展示。
10. 页面在 1366px 宽度下仍可正常使用。

## 28.2 功能验收

1. 可以新增订单。
2. 可以编辑订单。
3. 可以修改订单状态。
4. 可以上传文件。
5. 可以新增费用。
6. 可以确认费用。
7. 可以新增异常。
8. 可以关闭异常。
9. 可以维护物流节点。
10. 可以查看操作日志。
11. 可以批量导入订单。
12. 可以导出订单数据。
13. 权限不同，按钮和字段展示不同。
14. 关键操作有二次确认。
15. 风险订单能正常提示。

## 28.3 交互验收

1. 高频操作不超过 2 次点击完成。
2. 查询区默认只展示一行高频字段。
3. 更多筛选在右侧抽屉内完成，不挤压表格主体。
4. 表格信息密度高但不混乱。
5. 状态、风险、费用、文件信息可快速识别（文字 + 徽章，不只靠颜色）。
6. 弹窗、抽屉、Tab 层级清晰。
7. 删除、作废、审批类操作必须有确认。
8. 系统反馈明确，包括成功、失败、部分成功。

## 28.4 契约与状态验收

1. 每个已实现的业务功能都能指向一份最小完整契约（功能 ID、角色、可见/可用条件、请求与幂等、成功结果、失败结果与现场保留、刷新范围）。
2. 全局状态矩阵中的适用状态（加载、空、无权限、校验失败、业务拒绝、网络失败/慢请求、超长数据、重复提交、批量部分失败）在真实路由上可复现、可恢复。
3. 权限拒绝有独立表现，不得以网络错误形式出现；无权限字段不渲染而非置空。
4. 1366×768、1280×720、1024×768 分屏、1920×1080 及 200% 缩放下完成真实路由检查，布局、横向滚动、焦点、反馈就近性均通过。
5. 每个路由页面有对应的类型化页面规格（pageSpec），页面动作引用功能契约 ID，并通过项目静态检查与构建。

